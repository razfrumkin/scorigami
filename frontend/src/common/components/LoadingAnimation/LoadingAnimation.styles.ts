import { tss } from 'tss-react/mui'

export const useStyles = tss.create({
    root: {
        width: '100%',
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
})
